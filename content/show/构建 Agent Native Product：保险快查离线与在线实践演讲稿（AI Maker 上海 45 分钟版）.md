---
title: 构建 Agent Native Product：保险快查离线与在线实践（AI Maker 上海 45 分钟版）
published: 2026-09-06
created: 2026-09-06
modified: 2026-09-06
draft: true
tags:
  - AI Agent
  - Agent Native
  - OneAgent
  - 保险科技
  - 演讲
---

> 本稿用于 `AI Maker` 上海分享，依据原演讲稿及截至 2026 年 9 月 6 日的文章记录重写。按 45 分钟净分享安排，不含问答；时间包含架构图讲解、案例停顿，不依赖现场演示。方括号内为讲者提示，文末资料索引不朗读。

## 00:00—05:00　`Agent Native` 的全图：交付这次任务，改进下一次能力

大家好，我是王艺辉，也叫晓灰。我参与保险快查的 `Agent` 系统建设。今天沿着一个具体产品，讲我们怎样组织 `Agent` 的执行、协作和知识供给，让它们共同完成用户任务。

先看一句用户会问的话。

“国庆去尼泊尔爬山，买什么保险？”

这句话很短，背后的工作却不少。需要理解目的地、活动类型，查产品覆盖范围，核对登山和救援限制。有些信息用户没有说，比如具体路线、海拔、年龄和预算。系统得知道哪些条件已经确认，哪些还要追问。

如果用户接着问“这款能覆盖我的高海拔徒步吗”，任务又变了。刚才是在找候选，现在要读这款产品的责任、条件和除外。一个搜索命中理由，还不足以支持这个结论。

我们需要让系统围绕这个意图持续工作。它可以查资料、使用工具、交接子任务，也要知道证据不足时停在哪里。这是我理解的 `Agent Native` 产品交付。

但只把这次请求做完，还少了一块。如果同一个条件总被漏掉，下一次请求为什么能做得更好？这就涉及工具、提示词、检索模型和评测规则怎样改变。

所以，今天先把整张架构图放出来。

[全图讲解预留约 60～90 秒。沿旅行请求，依次指出产品材料与研究成果、搜品与取证、执行底座和失败反馈的位置。结合下文解释两个平面。图中的模块表示职责，不表示已经拆成独立服务。]

| 架构位置 | 负责的工作 | 保险快查中的对应实践 |
| --- | --- | --- |
| 产品平面 `Product Plane`：认知供给 | 把分散材料和研究过程变成可复用资产 | 产品报告、原文证据、材料索引、离线查询与产品池 |
| 产品平面 `Product Plane`：产品交付 | 围绕当前需求消费资产，交付内容与交互 | `DIPG` 深度解读、`AgenticOne` 搜品与咨询 |
| 共同执行底座 | 让领域 `Agent` 能持续执行、用工具、管理上下文 | `OneAgent`，以及节点内部的 `Agentic Loop` |
| 跨模块协作契约 | 约定任务、交接、状态、证据、校验和事件 | `Agent Protocol`，落实到工具、状态与编排代码 |
| 生产平面 `Production Plane` | 从失败与反馈出发，修改并验证下一版能力 | 规则回灌、检索训练数据、分层评测、候选版本验证 |

[表格用于备稿和制图，不逐行朗读。产品平面内部画“认知供给 → 可复用资产 → 产品交付”；执行底座和协作契约作为支撑。生产平面接收反馈，修改供给、交付及其支撑能力。不要把五行画成五个串行步骤。]

这张图里有两个平面。`Product Plane` 处理用户要获得的答案、内容和行动结果。`Production Plane` 处理产生这些结果的系统，包括代码、工具、提示词、数据和测试。

这里容易混淆“生产”这个词。离线生成一份保险报告，产物仍然是业务内容，所以属于产品平面。修改报告生成器，让下一批报告少犯一种错误，才进入生产平面。

离线和在线回答的是工作在什么时候做。两个平面回答的是工作在改变什么。这是两组不同的划分。

我会先讲 `OneAgent` 怎样执行任务，再讲多个任务之间怎样交接。然后看这些执行留下了什么资产，页面和在线咨询怎样使用它们，最后回到下一版能力怎样验证。

原来那次分享，我把报告和证据叫作“离线认知资本”。后续实践让我更在意两件事：这些成果能否被别的任务真正使用，失败能否改变下一次执行。接下来的案例，都围绕这张图展开。产品交付已有具体链路记录；生产平面的完整自动迭代，仍然是我们继续建设的方向。

## 05:00—12:00　执行底座 `OneAgent`：让领域任务跑得起来，也停得下来

先把视线放到全图底部。研究一款产品、查找一条证据、校验一个页面，业务目标不同，但都需要一段可以持续执行的过程。

以产品研究为例。`Agent` 收到产品编号后，先列出材料，打开条款，再沿关键概念搜索。读到一项责任，会继续核对条件和例外。如果现有证据不够，就再查；如果原始资料缺失，就应该标明缺口。

这些动作的顺序不能全部提前写死。不同险种的材料结构不同，同一个词也可能出现在不同文件里。模型需要根据读到的内容决定下一步。

`OneAgent` 为这种领域任务提供共同的执行内核。里面有围绕目标行动的 `ReAct Loop`、工具调用、状态和虚拟文件，也有隔离上下文的子 `Agent`、中间件、检查点和事件流。业务接入时，再配置它能用的工具、阅读方法、输出格式和停止条件。

这里的 `One` 指共同内核。我们可以从这个内核派生报告研究、信息补充、取证、校验等不同角色。它们各自承担有限的任务，不需要一个主 `Agent` 同时记住所有业务细节。

真正影响执行质量的，是这个内核接上了什么样的工作环境。

早期给研究 `Agent` 的是通用文件工具。我们在指令里写，必须到产品素材目录读取，不要搜索无关文件。运行时它还是会弄错目录。提示词里再重复几遍，工具的访问范围也没有改变。

后来我们把接口改成产品素材工具。列材料、搜索、读取，都围绕 `prod_no` 工作。工具自己定位素材根目录，读取时校验真实路径是否属于当前产品；搜索默认带上下文行，避免只返回几个命中词。

模型知道自己在研究哪个产品，就能调用工具。它不用反复拼磁盘路径，程序也不用相信它一定记住了目录限制。

再看它拿到材料以后怎样读。

比如原文里出现“续保”，报告不能看到这个词就写出强结论。`Agent` 需要继续找“申请”“审核同意”等条件，核对它们怎样限制前面的表述。阅读方法要引导它寻找会改变结论的内容，而工具要让它能够打开那些内容。

因此，领域能力不只放在提示词里。指令规定研究目标和方法，工具提供可执行的动作，状态保存已经取得的材料和结果，运行时处理调用过程。少了其中一部分，模型就会反复依赖上下文里的临时记忆。

早期我们也考虑过把核保、理赔、产品观点、合规等大量能力都塞进主提示词。长提示词看起来全面，但当前任务未必需要全部内容，不同要求还可能互相干扰。

现在主任务保留必要的阅读、写作、引用和降级要求。专门的补充工作交给工具或者独立节点，让执行时的上下文更贴近当前职责。

这里还有一个架构选择。既然模型能决定下一步，整条链路是不是都应该让它自由规划？

我们的报告系统外层采用固定编排。下载素材以后，由 `Host` 生成报告，再进入内部信息补充、外部认知补充、最终校验和回调。每个阶段有前置检查，也有明确的结果处理。

节点内部允许 `Agent` 自主搜索、阅读、修订。节点之间已经稳定的先后关系，由 `Graph` 控制。

比如世界知识补充节点，可以自行决定怎样查找外部信息。但它只能修改约定章节，父图会检查并拒绝越界修改。它不能因为找到了新的表达，就顺手重写整份报告。

[展示报告链路。用一个展开的节点表示内部循环，其余节点收起；重点讲清节点内部自主、节点之间固定编排的边界。]

这种组合让我们能分别修改两类问题。材料读得不够深入，可以调整节点的阅读方法和工具。交接顺序、修改范围或者失败处理有问题，就改编排代码。排查时也有具体位置可以下手。

还有一条容易被忽视的边界，循环必须有结束方式。任务完成、资料不足、工具失败和预算用尽，是不同的结果，不能最后都变成一句“已完成”。

搜品研究有迭代上限，页面修复有重试上限；报告的最终校验失败后，则不再循环重写。具体停在哪里，取决于这项业务愿意为修复付出多少时间，以及继续修能不能解决问题。

所以我不会把重试次数做成一条适用于所有 `Agent` 的产品规则。共同内核提供执行能力，业务编排规定这次任务的完成条件。用同一个底座，也可以有不同的预算和停止策略。

回到全图，`OneAgent` 让一个领域任务具备执行能力。接下来要解决的是，执行过程中留下的结果，另一个模块能否接着使用。

## 12:00—19:00　协作契约 `Agent Protocol`：让模块能接力，而不用共享全部对话

假设第一份 `report.md` 已经写出来，接下来要补充外部信息。最直接的做法，是把主 `Agent` 的整个对话历史复制过去，让另一个 `Agent` 继续写。

这份历史里会有搜索尝试、重复片段、尚未确认的推断，也可能有已经放弃的写法。补充节点需要的是当前报告、相关材料、自己的修改范围，而不是重新经历一遍所有过程。

这类协作需要六类约定，我们在系统里把它们统称为 `Agent Protocol`。这里说的是工程契约，并非行业标准。

前面这个交接，首先要讲清任务。补充哪部分信息，允许读取哪些来源，预期写回哪个章节，什么情况下可以不修改。这些内容决定接收方到底要完成什么。

然后交接可继续工作的状态。我们传任务描述、状态或虚拟文件引用，以及预期输出。各节点可以有独立上下文，通过 `report.md` 接力。收到引用的节点，再读取自己需要的内容。

任务契约和交接契约，就落在这些输入里。前者规定要做什么，后者保证接收方拿得到继续工作的材料。

状态契约还要进一步回答，什么结果值得保存。

模型上下文是当次推理的工作空间。已经形成的报告、取到的证据、校验结果，需要进入可以被后续步骤读取的状态。在报告链路里，虚拟文件由状态中的文件数据承载，不能只留在某一轮模型输出里。

最近我又把这件事拆得更细。检查点用于恢复某个执行状态，轨迹记录帮助还原调用和判断过程，较大的报告或素材则可以作为独立产物管理。后续设计中，状态保留引用，需要推理时再装载内容，可以减少重复保存大段文本。

但这几种记录各有用途。只有检查点，未必能完整还原每一次工具尝试；有一份最终报告，也不知道它经历了哪些修改。这部分分层存储，我把它作为后续设计来讨论；已有链路的实践，仍然要分别看。

继续沿着那份报告往后走。补充节点写入了一句结论，校验方怎样判断能不能用？

需要证据契约。结论来自哪份材料、哪个产品、哪段原文，要能够追溯。内部理赔信息、保险条款和外部世界知识的作用不同，来源身份不能在拼接报告时丢失。

比如外部介绍可以帮助解释产品名称，具体责任条件仍然要核对产品原始材料。把一段外部描述写得很肯定，并不会增加它对当前产品的证明力。

在线取证也一样。子 `Agent` 取得的证据写入 `search_evidence`，交给负责最终回答的模块使用。最终生成者要读证据、组织引用，不能因为主 `Agent` 看过一个搜索摘要，就跳过这一步。

接着是校验契约。调用成功只能说明程序走完了，产物能否交付还要单独判断。结果至少应当让下游区分通过、致命缺失和可保留的改进建议。

报告缺少核心条款，当前链路会作为错误处理，不交给下游当成完整结果。表达还有改进空间、但不影响交付的内容，可以保留为质量建议。调用方要根据这些不同结果继续编排。

统一的是结果要能被明确处理。具体失败以后重试、降级还是停止，由各自的业务契约决定。

最后一类是事件契约。用户在界面上看到“正在搜索”“正在取证”“生成回答”，后台可能正在运行多个嵌套节点。内部事件的先后到达顺序，不一定适合直接展示。

`AgenticOne` 的运行时会识别不同子图和调用的事件，再通过有序消费转换成业务事件。前端消费的是产品能够解释的过程，而不是一整棵内部调用树。

这里要特别分清几个编号。一次内部调用的 `run_id` 可以帮助定位运行关系；面向客户端的事件编号用于标识事件；检查点用于定位状态。一个编号的存在，不能代替另外两套语义。

如果产品需要断线补发，还得有实际保存的事件记录和消费位置。状态可恢复，并不会自动让前端接回遗漏的消息。我们讨论事件协议时，要把这个责任讲清楚，不能在架构图上只画一条流式输出箭头。

[回到全图，在供给与交付之间标出任务、状态引用、证据和校验结果；在交付与界面之间标出业务事件。六类契约可作为图例，不再逐项复述。]

有了这些约定，才能让模块分别演进。报告研究更换模型，补充节点仍然可以围绕同一份产物工作；在线回答调整表达方式，也不必重新承担产品材料的全部研究。变更有没有越过边界，可以在交接处检查。

## 19:00—27:00　认知供给：留下可以复用的研究成果

[在全图上突出“认知供给 → 可复用资产”，其余模块淡化。]

用户今天问一款保险，明天另一个用户又问同一款保险。两次问题不完全相同，但它们面对的条款、投保须知和增值服务材料，大量是相同的。

如果每次都重新下载、通读、比较，线上就要反复承担同一项研究的成本。我们把其中可以提前完成的部分移到离线，整理成后续任务可以继续使用的资产。

刚才那条报告链路以产品编号为入口。到了供给层，我们关心它最后留下哪些东西，能由谁继续使用。

这里输出的不只有一篇长文。报告组织了产品全貌，原文证据保留判断依据，来源记录帮助回查，索引提供进一步阅读的入口。后面的页面和咨询，需要的组合并不相同。

页面要面向普通用户组织完整介绍，报告是很好的起点。在线用户追问一个具体条件，报告可以帮助定位，但必要时仍然要回到原文。供给层不能要求所有消费者把整份报告重新读一遍，也不能承诺一份报告已经回答所有未来问题。

这也是后来补上 `QA Matching` 的原因。

我们已经拿到当前产品的材料，但用户用的是“赔付金额”，原文可能写“给付保险金”或者“年度累计限额”。只靠关键词，有时找不到真正相关的位置。

材料下载完成后，中间件建立产品范围内的语义索引。研究 `Agent` 查询时，先用产品编号限定范围，再取得前五个文本块编号，按编号读取原文，继续查找上下文和限制。

它交付的是阅读入口。某个块能够把 `Agent` 带到责任条款附近，就完成了一项有用工作；最后能写出多确定的结论，还要经过后面的阅读和核验。

我们第一版采用两千字窗口、两百字重叠，向量化文本块正文。这是已使用的切分方式，还没有系统证明它优于其他策略。相邻块重复、标题信息不足、识别噪声，都需要用具体查询继续验证。

索引构建失败时，原始材料仍然在。工具给出降级提示，研究过程继续用关键词搜索和原文读取。语义索引帮助提高找资料的效率，不应当变成材料明明存在却完全不能工作的单点。

再看另一种资产，它让供给层比原来多走了一步。

过去保存的是“这款产品有什么”。后续我们开始保存“针对这个需求，研究后有哪些候选产品”。离线的 `Agentic Search` 可以围绕复杂查询调工具、展开条件、搜索和过滤，把查询及其产品池存入检索系统。

在线遇到表达不同但选品意图相近的问题，就有机会复用这次已经完成的工作。

比如用户说“三高人群医疗险”，另一个人说“有高血压、高血糖、高血脂，想找医疗保障”。如果它们对应的选品条件足够接近，后者可以借助前者的离线产品池起步。

但如果只把“医疗险”改成“重疾险”，句子看起来几乎一样，候选产品范围已经变了。年龄、职业、疾病和保障方向中的一个关键条件，都可能决定一个池能不能复用。

因此，这份资产的有效性依赖原查询的业务意图。在线系统必须判断需求是否匹配，再检查当前上架、渠道等硬条件。离线研究留下的是候选基础，不能替代这一轮的资格核对。

[并排展示两种资产：产品编号对应报告与证据；离线查询对应候选产品池。用两条不同的消费连线，避免将产品池画成报告的固定下游。]

两类资产的更新条件也不同。条款变了，要重新核对报告中的结论；产品下架或者渠道变化，要重新检查产品池里的候选。每次消费都要知道这份结果来自什么材料、对应什么需求。

这也影响资产存储的设计。只保存最终一段话，下游很难判断它是否适用；把全部对话都保存下来，下游又要重新理解一遍过程。我们希望保留面向任务的报告、证据和结果引用，同时把诊断需要的轨迹留给排查与评测。

报告、材料索引和离线产品池有各自的存储与读取方式。复用时，需要通过产品编号、来源和任务信息识别资产，再执行对应的有效性检查。

供给层还需要拒绝产出看似完整的假结果。原始资料缺少关键事实，应该把缺口保留下来。模型把章节填满，并没有让材料更充分。

报告系统因此把交付门禁和质量打分分开。致命问题决定能否交付；评分帮助比较事实、覆盖和基础质量。材料的信息量也决定可达到的上限，不能要求只有少量公开资料的任务，与完整条款齐备的任务生成同等细节。

离线工作的时间预算相对宽松，适合做较深研究和校验，但仍然要算账。模型调用、工具、重试、人工复核，以及资产更新，都是成本。只有后续请求实际使用这些成果，提前研究才有机会摊薄开销。

现在供给已经交出报告、证据、索引和产品池。它们怎样进入一个用户看得到、等得起的产品，是下一层的职责。

## 27:00—38:00　产品交付：`DIPG` 做内容，`AgenticOne` 完成当前意图

[在全图上突出两个消费者。左边是 `DIPG`，右边是 `AgenticOne`；两者按需读取资产，不画成先后步骤。]

`DIPG` 交付一份可以直接阅读的内容，`AgenticOne` 处理当前会话中的意图。它们消费同一批资产，但时间预算和校验方式不同。

用户打开产品详情页，希望看到一份已经组织好的解释。此时我们可以提前研究、生成、校验，再把内容发布到对应产品。用户发起在线咨询，问题和上下文刚刚发生，系统必须在这一轮理解、调度和回答。

先看 `DIPG`，产品深度解读页面生成器。

它接收产品材料和研究结果，组织成适合用户阅读的页面。主路径是在离线完成生成与校验，写入数据库，再按产品启用。用户打开页面时，直接消费已经准备的内容。

生成页面带来的责任比写一段文字更重。内容要有依据，页面结构也得能正常显示。我们记录过一个具体问题，模型多生成了一个孤立的闭合标签，影响了手机上的页面布局。

还有一次，页面里出现“优于市场 85% 同类惠民保”。这个数字很像一个有用的产品判断，却找不到相应的市场排名依据。只看文案流畅度，甚至会觉得它写得不错。

这两类问题要求不同的检查。结构问题可以交给程序检查；事实和比较判断，需要拿生成内容与原始资料、审计记录核对。检查对象是最终要展示的页面，不能只因为前面的研究报告通过了，就默认后续表达一定正确。

在这条链路里，`Host` 调用研究能力生成内容，`Verify` 给出校验意见。失败时，`Host` 根据 `fix_hint` 局部修改，再交回校验。通过就直接交付，不再绕回修补；重试达到上限，则返回错误交接处理。

这里采用有上限的修复循环，与刚才报告供给链路的最终门禁不同。页面中一个局部标签或者一句无依据比较，有机会被定点修复。报告缺少核心资料，反复重写通常不能补出资料。

还要区分路径。历史上存在只走研究、没有后置校验的实时兜底，不能把离线主路径的保障推广到所有输出。谈系统可靠性时，得说清楚哪一条路径接受了哪种检查。

`DIPG` 的模块边界由此很明确。它对最终页面负责，消费已有研究成果，并检查自己新增的表达和结构。供给层的质量，不能免除交付层的责任。

再看右边的 `AgenticOne`。

在线咨询里的主 `Agent` 保持比较轻。它识别意图、处理指代、路由到合适能力。用户要找产品，就进入搜品；用户追问某款产品的具体条件，就进入取证和回答。

长证据由取证子任务写入状态，再交给最终的 `qa_summary` 组织回答。这个输出是终末输出，主 `Agent` 不在后面再改写一遍。否则刚刚组织好的证据关系和限制表述，还要再经历一次不必要的变化。

运行时也承担会话纪律，比如本轮证据和推荐数据的清理、引用处理、事件转换。这些事情每轮都要执行，适合放进中间件，减少对模型临时记忆的依赖。

在线搜品后来有两项进展，分别回答了“结果能不能用”和“用户要等多久”。

先看硬条件。

假设用户找六十岁人群能投保的产品，系统先做语义召回，只取很少的候选，然后才过滤年龄。留下的结果可能很少，但真正符合条件的产品，也许根本没进入最初那批候选。

在这种情况下，后面过滤得再正确，也找不回提前截掉的产品。

我们的召回数据面采用版本化 `JSON` 分发，启动时加载成内存中的产品快照。每次查询由模型提取结构化条件，程序在完整候选域上执行过滤，再排序、去重和截断。产品方向可以合并候选，核保、预算、保障等条件继续取交集。

模型负责理解“用户说的是什么条件”，确定的年龄和价格比较由程序执行。多轮继续筛选时，也保留完整的上一轮候选池，不能只把界面展示的前几款拿来继续过滤。

这项选择有适用范围。在现有产品规模和数据结构下，内存扫描便于把条件逻辑做清楚；规模继续增长，就需要重新计算内存与计算成本。搜索引擎也可以完成这项工作，前提是硬条件作用于完整候选域，再做截断。

这里讲的是结构化条件怎样执行。接下来回到线上搜索入口，看它怎样选择结果、控制等待时间。

如果每次复杂搜品都从头运行多轮研究，等待时间会增长。前面供给层已经保存了离线查询及其产品池，在线的 `QQ Matching` 就负责判断这次需求能否复用其中一份成果。

这条链路用查询向量检索离线查询，只取排名第一的匹配项。相似度达到阈值时，取它关联的产品池；匹配未过阈值，则使用原有搜索结果。这里不把多个近似查询的产品池随意混在一起，否则不同需求的限制也可能被混合。

拿到池以后，仍然检查当前产品的可用条件。相似度解决“这次需求能不能借用以前的工作”，业务过滤解决“这些候选现在能不能用”。两者承担不同责任。

在线入口沿用的搜索分支是 `HA3 easy`。请求进入时，复杂度判断、`HA3 easy` 搜索和 `QQ Matching` 同时开始。简单查询提交 `HA3 easy` 结果；复杂查询且匹配命中，提交离线产品池；未命中则提交已经在运行的 `HA3 easy` 结果。

[预留约 30～45 秒，展示三条分支从同一时刻起跑，分别走一遍复杂查询命中与未命中的路径。指明未命中时，`HA3 easy` 已经在执行。]

这个改动减少了串行等待。它同时付出了额外计算，因为最后只使用部分分支的结果。这是一次用资源换响应时间的调度选择，需要结合请求量、分支成本和命中情况来判断。

文章记录的一天里，这个搜索接口的算术平均耗时约为三百四十毫秒，百分之九十五的请求在六百四十毫秒以内。这里说的是该接口的观测值，不是整段咨询的耗时，也没有足够的前后对照用来推导提升比例。

这组实践让我对在线 `Agent` 的职责有了更具体的理解。它需要决定哪些工作值得当场做，哪些可以使用已有成果，哪些条件仍然必须现场核对。供给层准备得越充分，在线越有条件把时间留给用户这一次的新信息。

回到尼泊尔爬山的例子。产品池可以帮它更快找到候选，报告帮助理解产品，材料索引帮助定位相关条款。但用户没说的海拔仍然没说，找到“登山”两个字也仍然不够。

交付层需要把已确认条件、候选结果和待补信息组织成用户能继续行动的回答。用户追问某一款时，再沿具体证据继续工作。这个请求最终能走多远，要由材料和当前信息共同决定。

## 38:00—45:00　能力演进：让失败改变下一版执行

[回到全图，突出 `Production Plane`。从交付结果、校验记录和执行轨迹连入；向工具、提示词、数据及评测规则画出修改关系。已记录的实践与后续方案使用不同标记。]

已经记录过页面写出无依据百分位的问题。搜品和取证还需要检查另外两类失败，例如年龄条件不符，或者定位正确却漏读了后面的限制。

修好眼前这个结果，任务可以结束。但下一次遇到类似问题，系统接收的输入如果完全相同，使用的工具和检查也没有变化，它仍然可能再犯。

生产平面要处理的，就是这些失败怎样进入下一版能力。

我们在页面系统里做过半自动的规则回灌。比如没有依据的市场排名，不能只在当前页面删掉那一句，还要形成后续生成与校验会读取的约束。外部公司名称和当前产品主体混淆，则要补充实体对齐规则。

这只是改进的开始。加了规则以后，还要重新跑相关样本，检查同类错误是否减少，原本正常的内容有没有被误伤。规则变长，不等于系统变好。

刚才改的是生成规则。再看图里的数据这一项，`QQ Matching` 的匹配失败，会成为制作正负样本的依据。最近的数据方案，就围绕会改变保险选品结果的条件展开。

同一个“三高医疗险”需求可以展开成更完整的疾病表述，用作正例。把医疗险换成重疾险，或者把六十岁换成六岁，就可以构造保留大量相同文字、却改变关键条件的难负例。

样本不能只追求看起来相似。产品名称、别名和条件，需要通过产品数据核对；两个需求存在包含关系时，也不能草率判成负例。训练集和测试集还要按语义与别名分组隔离，避免同一意图换个说法就跨进测试集。

已有线上链路使用过领域微调的向量模型；这里新的训练数据与 `Qwen3-Embedding` 方案，还没有新的训练效果对照。它展示的是怎样从复用失败中提炼训练任务，尚不能作为模型效果提升的成果。

无论修改的是规则还是数据，效果都要回到各模块的交付物上检查。

`QQ Matching` 要看最终带回的产品是否符合用户条件。查询之间的向量距离变近，只能解释中间变化。文中采用产品层面的 `Precision@3`，按每条查询计算，再汇总比较。

`QA Matching` 要看前五个定位结果能否帮助找到相关材料，用的是文档导航的口径。但导航正确以后，`Agent` 仍然可能遗漏除外，或者把条件写错，所以最终报告和答案还要单独检查。

报告评测再看事实、覆盖和基础质量，并将合规等门禁单独处理。检查项可以使用明确的离散分值，算分交给程序。这样一次退步能够追到具体项目，而不是只得到一句“整体不够好”。

评分器本身也会出错。它可能偏爱更长、更像报告的文本，也可能在重复运行中给出不同判断。我们需要用专家样本校准，并检查稳定性。如果同时换了生成模型和评分器，新旧总分就不能直接当作同一把尺子下的进步。

[展示一个失败样本如何回到对应模块：产品条件错，查召回与匹配；原文定位错，查导航；证据正确但结论错，查阅读与生成；页面显示错，查渲染产物。不要虚构新旧分数。]

我在 `Harbor` 的实验和相关文章里继续研究了候选版本验证。希望固定任务材料、运行环境和评测器，让不同版本在可比较的条件下执行，再检查具体任务的变化。

实验中已经遇到过一个很实际的区别。空操作代理跑完任务，得到零分，这是有效的失败结果；环境因为容器凭据辅助程序的问题没有正常启动，则是基础设施错误。后者不能混进模型能力分数，也不能因为命令退出就算完成验证。

再往发布走，候选版本要有明确身份，评测与上线使用的制品要能对应起来。失败轨迹要保留，才能知道应该改工具、提示词还是数据。这一整套自动验证与晋级是继续建设的方案，已有的本地实验还不能代表完整生产链路已经自动运行。

自动修好一页内容，证明了局部修复能工作；让下一批页面少犯同类错误，还需要规则进入执行，再拿结果验证。

现在回到开场的用户。

他看到的是一句回答或者几款候选，背后需要这些模块共同负责。`OneAgent` 执行领域任务，契约规定交接边界，供给层留下可以复用的研究成果，交付层围绕本轮意图使用它们。失败再进入评测和修改，才可能改变下一次执行。

对正在做产品的朋友，我建议拿一个真实请求检查这张图。重复读的材料保存在哪里，结论能否回到证据，硬条件由谁执行，失败最后会改动哪个文件、哪条规则或者哪组样本。哪个位置没有答案，就先从那里补。

下一次再分享时，我希望能拿出一组可比较的记录。同一类需求，新旧版本分别返回了什么，哪条限制过去漏掉了，修改以后有没有改善，又多花了多少时间和成本。

谢谢大家。

---

## 资料索引与事实口径（不朗读）

本稿沿用原稿的架构，把后续进展放回对应模块；以下区分已有实践、后续增强与设计探索。旧图片式幻灯片不作为事实来源。

- 原始分享与定义：[[构建 Agent Native Product：保险快查离线与在线实践演讲稿]]、[[Agent-Native|Agent Native：让产品形成智能闭环]]。保留 `OneAgent`、六类契约、认知供给、`DIPG` 和 `AgenticOne`；离线报告生产仍属产品平面，修改生成器等能力属于生产平面。
- 执行底座与报告供给：[[保险产品解读报告系统：Multi-Agent 生产链路与评测闭环]]。外层固定编排、素材工具范围、上下文隔离及报告链路取消重试，6 月文章已有记录，不作为会后新增成果。`QA` 材料导航属于后续补充。
- 页面交付：[[Harness Engineering - C 端 AIGC 实时生成系统]]。孤立闭合标签、无来源“85%”、`Host` 局部修补后重验、半自动规则回灌均来自记录。页面修复最多重试五次；报告链路的最终校验不重试。历史实时兜底的保障与离线主路径分开。
- 在线执行与协作：[[AgenticOne：OneAgent 范式在保险实时咨询中的应用]]。轻主 `Agent`、`search_evidence`、`qa_summary` 终末输出及事件转换来自该文。不把内部 `run_id` 当作客户端唯一事件编号，不推导检查点天然支持事件补发。
- 资产复用与时延：[[RAG 工程实践：QQ 产品召回与 QA 文档导航]]。`QQ` 选择单个离线查询及其产品池；`QA` 返回前五个文本块编号后继续读取。`340ms` 为文中某一天的接口算术平均耗时，`640ms` 为同口径 `P95`；原文未给具体统计日期、样本量及前后对照。正式幻灯片如保留指标，应补齐统计口径；不写成整段咨询耗时、当前服务承诺或提升幅度。
- 召回数据面：[[AgenticOne 产品召回：JSON 分发与内存全量过滤]]。完整候选域硬条件过滤，与 `QQ` 查找离线查询承担不同职责。内存快照是既有规模下的实现选择，不推导为任何规模都优于搜索引擎。
- 匹配模型演进：[[embedding-training-for-qq-matching|保险推品中的语义匹配：从产品池复用到领域微调]]。既有领域微调模型与新的 `Qwen3-Embedding` 训练数据方案分开；新方案没有训练效果对照。
- 状态与产物：[[agent-checkpoint-trajectory-storage|Agent Checkpoint 与 Trajectory 的分层存储]]。状态、轨迹及大产物引用分层属于架构讨论，不声称所有保险快查链路已经统一实现。
- 分层评测与版本验证：[[如何科学评测 Agent 生成的文本报告：从评分体系到评测器的元评测]]、[[用 Harbor 构建稳定的 Agent 评测与进化环境]]。业务评测、本地实验与完整自动晋级方案分别叙述；不用示例分数、门禁阈值替代实际业务成果。
- 演进与成本：[[ai-system-evolution-generator-evaluator-optimizer|AI 系统如何进化：生成器、评估器、优化器的关系]]、[[AI 应用公司的利润从哪里来]]。局部修复、下一次执行条件改变、改进策略验证分开；没有未经测量的成本节省比例。
